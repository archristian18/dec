import React from 'react';
import useStyles from './style'

export const Rightbar = () => {
    
    const classes = useStyles()

    return (
        <div className={classes.container}>Rightbar</div>
    );
};
